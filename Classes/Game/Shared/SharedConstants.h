#ifndef SharedConstants_h
#define SharedConstants_h

static const int DEFAULT_TICK_RATE = 10;
static const float DEFAULT_DELTA_TIME = 1.f / DEFAULT_TICK_RATE;
static const float DEFAULT_INTERPOLATION_LATENCY = DEFAULT_DELTA_TIME * 3.f;
static const float ITEM_GRAB_RADIUS = 40.f;
static const float EXPLOSION_RADIUS = 64.f;
static const float DEFAULT_RELOAD_TIME = 1.f;
static const float DEFAULT_HEADSHOT_DAMAGE_MULTIPLIER = 1.5f;

#endif /* SharedConstants_h */
